<div class="row">
  <div class="col">
    <%= render "card_task", title: "Not yet started" %>
  </div>
  <div class="col">
    <%= render "card_task", title: "In progress" %>
  </div>
  <div class="col">
    <%= render "card_task", title: "Completed" %>
  </div>
</div>


<!--- 




<h1>Tasks</h1>

<table>
  <thead>
    <tr>
      <th>Text</th>
      <th>Status</th>
      <th>Owner</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.text %></td>
        <td><%= task.status %></td>
        <td><%= task.owner_id %></td>
        <td><%= link_to 'Show', task %></td>
        <td><%= link_to 'Edit', edit_task_path(task) %></td>
        <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Task', new_task_path %>

--->
